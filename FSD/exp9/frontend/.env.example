# Optional: set this when your backend runs on a different origin (for example 8080)
# If omitted, the CRA proxy in package.json forwards /api requests to localhost:8082.
REACT_APP_API_BASE_URL=http://localhost:8080
